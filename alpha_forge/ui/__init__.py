"""Terminal presentation components over application events.

Framework boundaries are marked at their definitions: @override for inherited
methods, widget-protocol docstrings for __pt_container__, and callback docstrings
for explicitly registered functions. Our handle/on_change/focus_target API is
application-owned. See docs/ui-architecture.md for the framework boundary map.
"""
